import styled from 'styled-components';

const CalculatorEl = styled.div`
  background-color: #d8eefe;
  padding: 20px;
  height: 500px;
  border-radius: 10px;
  flex: 1;
`;

const Calculator = () => {
  return <CalculatorEl>Calculator</CalculatorEl>;
};

export default Calculator;
